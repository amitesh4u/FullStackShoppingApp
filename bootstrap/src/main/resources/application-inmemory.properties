spring.datasource.url=jdbc:h2:mem:testdb
persistence=inmemory

appId=ShopApp
server.port=8080
server.servlet.context-path=/shop

# Setting a new path for api docs
#springdoc.api-docs.path=/custom-api-docs

# Setting a new path for Swagger UI
#springdoc.swagger-ui.path=/swagger-ui-custom.html

# Sort the API paths according to their HTTP methods
springdoc.swagger-ui.operationsSorter=method

# To allow CORS calls from different server
cors.front-end.url=http://localhost:3000
